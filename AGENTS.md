请为我编写一个完整的Python程序，用于双臂分拣机器人的D-H参数建模与仿真。这个程序将用于我的毕业设计"基于VLA的产线瑕疵品分拣关节机器人设计"。
词程序为机械设计前的最简运动学仿真

## 一、系统架构设计

### 1.1 机械臂配置
- **双臂系统**：两台独立的6自由度机械臂协同工作
- **单臂结构**：6个旋转关节 + 1个末端执行器（夹爪）
- **关节配置**：
  - J1：基座旋转（绕Z轴）
  - J2：肩关节俯仰（绕Y轴）
  - J3：肘关节俯仰（绕Y轴）
  - J4：前臂旋转（绕Z轴）
  - J5：腕关节俯仰（绕Y轴）
  - J6：末端旋转（绕Z轴）
  - 夹爪：开合运动（视为第7个自由度，但不参与D-H参数建模）

### 1.2 技术指标（来自任务书）
- 臂展：≥ 500mm
- 负载：≥ 5kg
- 重复定位精度：±0.5mm
- 工作空间：需要覆盖分拣区域（传送带长度约1000mm）

## 二、D-H参数建模要求

### 2.1 改进型Denavit-Hartenberg (MDH) 参数定义
使用改进型D-H参数表，每个关节包含4个参数：
- **θ（关节角度）**：绕Z轴的旋转角（可变参数）
- **d（连杆偏距）**：沿Z轴的位移
- **a（连杆长度）**：沿X轴的位移
- **α（连杆扭转角）**：绕X轴的旋转角

### 2.2 变换矩阵公式
每个关节的齐次变换矩阵：T_i = Rot_z(θ_i) * Trans_z(d_i) * Trans_x(a_i) * Rot_x(α_i)
末端执行器相对于基座的位姿：T_0_6 = T_0_1 * T_1_2 * T_2_3 * T_3_4 * T_4_5 * T_5_6


## 三、代码模块设计

请实现以下Python类和函数：

### 3.1 核心类：RobotArm6DOF
### 3.2 双臂系统类：DualArmSystem
### 3.3 分拣场景类：SortingScenario

## 四、参数设置（示例值，可根据实际设计调整，调整处给足注释说明引导）
MDH参数表（单位：米、弧度）
格式：[θ, d, a, α]
MDH_PARAMS_EXAMPLE = [
[0.0, 0.15, 0.0, np.pi/2], # J1: 基座
[0.0, 0.0, 0.30, 0.0], # J2: 肩部
[0.0, 0.0, 0.25, 0.0], # J3: 肘部
[0.0, 0.10, 0.0, np.pi/2], # J4: 前臂
[0.0, 0.0, 0.0, -np.pi/2], # J5: 腕部
[0.0, 0.10, 0.0, 0.0] # J6: 末端旋转
]

关节限位（单位：弧度）
JOINT_LIMITS_EXAMPLE = [
[-np.pi, np.pi], # J1
[-np.pi/2, np.pi/2], # J2
[-np.pi/2, np.pi/2], # J3
[-np.pi, np.pi], # J4
[-np.pi/2, np.pi/2], # J5
[-np.pi, np.pi] # J6
]

夹爪配置
GRIPPER_CONFIG = {
‘max_width’: 0.10, # 最大开合宽度：100mm
‘max_force’: 50, # 最大夹持力：50N
‘type’: ‘parallel’ # 平行夹爪
}

双臂偏移（右臂相对于左臂）
ARM_OFFSET = [0.6, 0.0, 0.0] # X轴偏移600mm

传送带配置
CONVEYOR_CONFIG = {
‘length’: 1.0, # 长度：1米
‘width’: 0.3, # 宽度：300mm
‘height’: 0.1, # 高度：100mm
‘speed’: 0.1 # 速度：0.1m/s
}

相机配置
CAMERA_CONFIG = {
‘top_view’: {‘position’: [0.5, 0, 1.0], ‘orientation’: [0, -np.pi/2, 0]},
‘front_view’: {‘position’: [0.8, 0, 0.5], ‘orientation’: [0, 0, -np.pi/4]}
}

## 五、可视化要求

### 5.1 机械臂可视化
- 使用matplotlib的mplot3d或PyBullet
- 每个关节用圆柱体表示，连杆用线条表示
- 显示坐标系（X-红色，Y-绿色，Z-蓝色）
- 夹爪用方块表示，开合状态用颜色表示

### 5.2 场景可视化
- 传送带用绿色矩形表示
- 正常产品用蓝色球体表示
- 瑕疵产品用红色球体表示（尺寸不同）
- 相机视野范围用黄色线框表示

### 5.3 动画演示
- 分拣过程动画：从初始位置→移动到产品→抓取→移动到放置区→释放
- 双臂协调运动演示
- 工作空间可视化